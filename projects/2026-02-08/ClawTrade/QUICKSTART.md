# ⚡ 快速启动指南（2 小时部署版本）

## 📍 当前进度

✅ **代码已完成**
- 后端 API（买入/卖出/持仓/历史/OpenClaw）
- 前端 UI（连接后端版本）
- 数据库 Schema
- 测试文档

---

## 🎯 接下来你需要做的事（按顺序）

### ⏱️ 10 分钟 - 创建数据库

**推荐使用 Supabase（最快）**：

1. 访问 https://supabase.com
2. 点击 "New Project"
3. 填写项目名称：`clawtrade`
4. 设置数据库密码（记住它！）
5. 选择地区：Singapore（离中国最近）
6. 等待 2 分钟创建完成
7. 进入 Settings → Database → Connection String
8. 复制 "URI" 格式的连接字符串，格式类似：
   ```
   postgresql://postgres.xxx:password@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
   ```

---

### ⏱️ 5 分钟 - 配置并启动后端

```bash
# 1. 进入后端目录
cd server

# 2. 安装依赖
npm install

# 3. 创建 .env 文件
cat > .env << 'EOF'
DATABASE_URL="你从 Supabase 复制的连接字符串"
PORT=3001
CORS_ORIGIN="http://localhost:5173"
NODE_ENV=development
EOF

# 4. 生成 Prisma Client
npm run prisma:generate

# 5. 推送数据库模式（创建表）
npm run prisma:push

# 6. 初始化数据（创建 demo 用户）
npm run seed

# 7. 启动后端服务器
npm run dev

# 看到以下输出表示成功：
# 🚀 ClawTrade API Server 启动成功
# 端口: 3001
```

**保持这个终端窗口运行！**

---

### ⏱️ 3 分钟 - 启动前端

**新开一个终端窗口**：

```bash
# 1. 进入前端目录
cd client

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 看到输出：
#   VITE v5.x.x  ready in xxx ms
#   ➜  Local:   http://localhost:5173/
```

**访问 http://localhost:5173**

---

### ⏱️ 5 分钟 - 功能测试

在浏览器中测试以下功能：

1. **行情页**：
   - ✅ 显示 12 个币种
   - ✅ 显示实时价格（从后端 API 获取）
   - ✅ 点击"交易"按钮跳转到交易页

2. **交易页**：
   - ✅ 输入金额（如 1000）
   - ✅ 点击"买入 BTC"
   - ✅ 看到成功提示："✅ 买入 0.0123 BTC @ $81,000"
   - ✅ 余额从 $100,000 变为 $99,000

3. **持仓页**：
   - ✅ 显示刚才买入的 BTC 持仓
   - ✅ 显示浮动盈亏
   - ✅ 资产饼图正确显示

4. **历史页**：
   - ✅ 显示刚才的买入记录

5. **AI 建议**（交易页）：
   - ✅ 点击"🤖 AI 建议"按钮
   - ✅ 弹出 OpenClaw 建议面板
   - ✅ 显示 Mock 建议（买入/卖出/观望）

---

## 🐛 如果遇到问题

### 后端启动失败：`Error: P1001: Can't reach database server`

**原因**：数据库连接字符串错误

**解决**：
1. 回到 Supabase，重新复制连接字符串
2. 确认密码正确
3. 重新编辑 `server/.env` 文件

### 前端无法连接后端：Network Error

**原因**：后端未启动

**解决**：
1. 检查后端终端是否显示"服务器启动成功"
2. 访问 http://localhost:3001/health 测试后端
3. 如果无响应，重新启动后端

### 买入交易失败：`余额不足`

**原因**：数据库未正确初始化

**解决**：
```bash
cd server
npm run seed  # 重新初始化 demo 用户
```

---

## 📦 部署到生产环境（可选）

### 后端部署到 Railway（10 分钟）

1. 访问 https://railway.app
2. 连接 GitHub 仓库
3. 选择 `server` 目录
4. 添加 PostgreSQL 插件（Railway 自动配置 DATABASE_URL）
5. 设置环境变量：
   - `CORS_ORIGIN=https://你的前端域名.vercel.app`
6. 部署完成后，记录后端 URL（如 `https://clawtrade.railway.app`）

### 前端部署到 Vercel（5 分钟）

1. 访问 https://vercel.com
2. 导入 GitHub 仓库
3. Root Directory: `client`
4. 设置环境变量：
   - `VITE_API_URL=https://clawtrade.railway.app`
5. 点击 Deploy

---

## ✅ 测试清单

完成以下测试后，你的 Demo 就可以演示了：

- [ ] 后端 API 正常运行（http://localhost:3001/health）
- [ ] 前端页面正常访问（http://localhost:5173）
- [ ] 能成功买入 BTC
- [ ] 能成功卖出 BTC
- [ ] 持仓页显示正确
- [ ] AI 建议功能可用
- [ ] 交易历史正确记录

---

## 🎉 完成！

恭喜你完成了 ClawTrade V2 的本地部署！

**下一步**：
- 测试所有功能
- 部署到生产环境（Vercel + Railway）
- 接入真实 OpenClaw Agent

如有问题，查看完整文档：[README.md](README.md)
