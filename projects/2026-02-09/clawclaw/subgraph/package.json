{
  "name": "agentbridge-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for AgentBridge",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio agentbridge-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
